# ASL-Translator
This project implements a real-time American Sign Language (ASL) gesture recognition system using computer vision and machine learning. The system detects hand landmarks using MediaPipe and classifies gestures using a trained Random Forest model.
